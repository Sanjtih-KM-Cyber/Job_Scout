# frontend/.env.example
# Copy to .env.local

# Backend API URL — in dev, Vite proxies /api → localhost:4000 automatically
# In production, set this to your deployed backend URL
VITE_API_URL=https://your-backend.railway.app/api
